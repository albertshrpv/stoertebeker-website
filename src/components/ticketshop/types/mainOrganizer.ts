export interface MainOrganizerData {
    id: string;
    // System fee fields
    system_fee_amount?: number;
    system_fee_percentage?: number;
    system_fee_currency: string;
    system_fee_vat_rate: number;
}